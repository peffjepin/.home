
shift + ctrl {Left,Right,space}
    ,spotifyctl {prev,next,toggle}

shift + ctrl {Up,Down}
    ,audioctl {up,down}

shift + alt + ctrl + Down
    ,audioctl mute

super + Return
    $TERMINAL

super + p
    $TERMINAL ipython

super + i
    $BROWSER

super + m
    ,cycle_sinks
